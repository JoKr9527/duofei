spring:
  application:
    name: config-client
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: prod
      label: master
      name: duofei
      username: duofei
      password: duofei001
server:
  port: 60022
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1113/eureka/
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: refresh