spring:
  cloud:
    consul:
      config:
        enabled: true
        prefix: configuration
        defaultContext: consul-server-register
        profileSeparator: '::'
        data-key: config
        format: yaml