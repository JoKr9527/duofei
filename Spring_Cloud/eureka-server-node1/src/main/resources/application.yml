server:
  port: 1112
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:1113/eureka/
  server:
    my-url: http://localhost:1112/
    enable-self-preservation: false
spring:
  application:
    name: eureka-server-node1
